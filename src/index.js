import React from 'react';
import ReactDOM from 'react-dom'; // estas librerias permite que funcione la aplicacion 
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

